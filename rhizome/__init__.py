"""Rhizome — a research maturation engine.

Deterministic Python that reads/writes a markdown+YAML vault. This package will hold the
engine (layers, gates, structural matcher, nondimensionalization, principle condensation).
For now it ships the model-selection layer (`rhizome.models`).
"""

__version__ = "0.0.1"
